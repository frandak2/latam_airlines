# conda env create --file environment.yml
# mamba env create --file environment.yml
name: latam_airlines
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - black
  - invoke
  - jupyter
  - jupyterlab
  - matplotlib
  - nbdime
  - numpy
  - pandas
  - pip
  - plotly
  - pyjanitor
  - pyprojroot
  - python=3.10
  - seaborn
  - scikit-learn
  - pip:
    - xgboost
    - imblearn
    - LIME